# My Project
Create repo with your name like "firstname-lastname" 
Add a file with your firstname contains one line "this is the first line" commit your work please specify the file status in each stage

Rename your current branch to be "main"

Add a new file with your lastname contains "my last name is ......" And commit your work

Make a change in your firstname file and restore the old copy to discard the changes

Revert to the first commit with two ways and explain the difference 

Now go forwarded to the second commit with two ways 

Show the difference between two commits

Create a tag for the second commit to be v1.0

Show the content of commit 

Create a remote repo on github and upload your local repo

Make a change on the remote repo and get that change to your local repo with two ways and explain the difference 

Create a two local branch from your main branch on the first branch  edit the first line on your firstname file to be "hello from the first branch" and upload this branch to the remote repo 

Now merge that change to the main branch and push it 

Now edit the first line on your firstname file to be "hello from the second branch" and merge that change to your main branch


Create a .gitignore file and ignore .env node_modules and log files
Create a .env file and verify git does not track it
Force add the .env file and commit it
Stop tracking .env without deleting it locally

Create a file called bio.txt and commit with wrong message then fix commit message without creating new commit
Add new content to same commit using amend
Explain when we should avoid amend

Modify two files and stage only part of changes using patch mode
Commit only staged changes
Show remaining unstaged changes

Show git history in these formats one line format graph format last 3 commits only commits by specific author commits affecting one file only

Create 2 new commits then reset using soft reset mixed reset hard reset Explain the difference between working directory staging and commits

Create branch feature-login add commit switch to main and cherry pick the commit
Explain when cherry pick is useful

Create branch feature-ui add two commits update main with new commit then integrate using merge
Repeat same scenario using rebase
Explain difference between merge and rebase

Create merge conflict by editing same line in two branches resolve conflict manually and complete merge
Explain conflict markers

Modify a file and stash changes
Create new branch apply stash drop stash list stashes
Explain when stash is useful
